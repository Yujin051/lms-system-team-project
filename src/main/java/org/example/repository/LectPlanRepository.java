package org.example.repository;

import org.example.entity.LectPlan;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LectPlanRepository extends JpaRepository<LectPlan, Long> {
}
