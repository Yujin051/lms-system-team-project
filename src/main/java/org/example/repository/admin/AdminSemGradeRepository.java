package org.example.repository.admin;

import org.example.entity.SemGrade;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AdminSemGradeRepository extends JpaRepository<SemGrade, Long> {
}
